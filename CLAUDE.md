# 언어 규칙

**반드시 한국어로만 답변할 것.** 사용자에게 보여지는 모든 응답 텍스트(설명, 요약, 질문, 안내 등)는 예외 없이 한국어로 작성한다. 영어 문장을 섞어서 답변하지 않는다.

- 코드, 커밋 메시지, 변수명 등 코드 자체의 관례는 기존 프로젝트 관례(영문 커밋 태그 `feat:`/`fix:`/`docs:` + 한국어 설명 등)를 따른다. 이 규칙은 사용자에게 보여지는 대화 응답에 적용된다.
- 사용자가 별도로 다른 언어를 요청하지 않는 한 항상 이 규칙을 지킨다.
